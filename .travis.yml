os:
  - linux
language: csharp
mono: none
dotnet: 2.1
before_script:
- dotnet restore
- dotnet tool install --global dotnet-sonarscanner --version 4.6.0
- export PATH="$PATH:$HOME/.dotnet/tools"
script:
- bash ./build/ci.sh