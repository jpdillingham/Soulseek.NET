version: 2
jobs:
  build:
    docker:
      - image: microsoft/dotnet:2.1.300-sdk
    steps:
      - checkout
      - run: find .
      - run: dotnet restore
      - run: dotnet build --no-restore --no-incremental
      - run: ./build/test.sh